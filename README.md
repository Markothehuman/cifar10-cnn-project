# CIFAR-10 CNN Project

This project trains and compares multiple CNN approaches on CIFAR-10, including:
- A baseline CNN from scratch
- An improved CNN with stronger augmentation and training recipes
- Transfer learning with a pretrained ResNet

Key scripts live in `SRC/` and notebooks in `Notebooks/`.
